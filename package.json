{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "deployment",
  "scripts": {
    "build": "mkdir -p dist && npx cpy-cli . dist && rm -rf dist/.git dist/.gitignore dist/node_modules dist/README.MD dist/CUSTOMIZING.md dist/CUSTOMIZING.md"
  },
  "dependencies": {
    "cpy-cli": "^5.0.0"
  },
  "devDependencies": {},
  "author": "reind",
  "license": "MIT"
}

